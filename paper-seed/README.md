# paper-seed

State the paper cycle starts from when this repository's Actions cache is empty. Rebuilt on 2026-09-16 from the last snapshot published by the private repository (seq 198, 2026-09-16 00:48 UTC): every closed trade of every book with entry, exit, high, reason and R; no open positions; entry cooldown maps derived from the trades. `entry_liq` is not in the published record and is null. The raw market panel (`market_log.jsonl`) could not be rebuilt and restarts from the first run here; the 14-day gate-passing and flow lists remain in the published `data.json`.
